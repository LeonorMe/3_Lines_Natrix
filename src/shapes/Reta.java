package shapes;
/*
public class Reta extends Shapes{
    Point[] points = {new Point(0,0), new Point(0,0)};

    public Reta(String name, Point p1, Point p2, boolean close){
        this.points = {p1, p2};
        float x = (p1.x + p2.x)/2;
        float y = (p1.y + p2.y)/2;
        super(name, x, y, close);
    }
    @Override
    public void move(int x, int y) {
        for (Point point: this.points) {
            point.x += x;
            point.y += y;
        }
    }

    @Override
    public void resize(float times) {
        for (Point point: this.points) {
            point.x *= times;
            point.y *= times;
        }
    }

    @Override
    public void show() {

    }

    @Override
    public void saveSVG() {

    }
}
*/