package Paint;

import javax.swing.*;
import java.awt.*;

public class styleControls extends JDialog {
/*
    private String styleInput = "stroke:black;stroke-width:1";

    public styleControls() {
        this.styleInput = JOptionPane.showInputDialog("Enter style (stroke:black;stroke-width:1) ");
        Paint.drawArea.setStyle(this.styleInput);

        Object complexMsg[] = { "Above Message", new ImageIcon("yourFile.gif"), new JButton("Hello"),
                new JSlider(), new ImageIcon("yourFile.gif"), "Below Message" };

        JOptionPane optionPane = new JOptionPane();
        optionPane.setMessage(complexMsg);
        //optionPane.setMessageType(JOptionPane.INFORMATION_MESSAGE);
        optionPane.setOptionType(JOptionPane.OK_CANCEL_OPTION);
        optionPane.setWantsInput(true);
        optionPane.setMessageType(JOptionPane.QUESTION_MESSAGE);
        JDialog dialog = optionPane.createDialog(null, "Width 100");
        dialog.setVisible(true);
    }

    public String getStyle() {
        //String[] parts = style.split(";");
        //g2.setPaint(Color.decode(parts[0].split(":")[1]));
        //g2.setStroke(new BasicStroke(Integer.parseInt(parts[1].split(":")[1])));
        //g2.fill();
        return styleInput;
    }

    */
}
